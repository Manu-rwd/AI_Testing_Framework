# Preserve exact bytes (BOM + CRLF) for emitted/fixture CSVs
tmp_exports/*.csv -text
tmp_review/*.csv -text
packages/planner/test/**/*.csv -text

# Everything else can stay normalized
*.ts text eol=lf
*.md text eol=lf
